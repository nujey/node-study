var str:string = '数组合并了相同类型的对象, 而元组合并了不同类型的对象'

let xcat: [string, number] = ['xcat', 25]

xcat.push("test")

xcat.push(true)

/**
 * @author shandian
 * @version v1.0.0
 * @param {Array} arr 数组
 * @returns {Object} 返回对象
 */
function test(arr) {

}