
var c: string = 'ECMAScript内置对象'

let b: Boolean = new Boolean(1)
let e: Error = new Error('Error')
let d: Date = new Date()
let r:RegExp = /^[a-z]$/

var c1:string = 'DOM和BOM内置对象'

let body: HTMLElement = document.body
let allDiv: NodeList = document.querySelectorAll('div')
